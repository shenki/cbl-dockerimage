sudo: required

language: c

services:
  - docker

matrix:
  include:
    - name: "Development"
      env: LLVM_VERSION=10
    - name: "Stable"
      env: LLVM_VERSION=9

script:
  - make release
  - make check

deploy:
  provider: script
  script: make deploy
  on:
    branch: master
